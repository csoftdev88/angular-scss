machine:
  environment:
    NODE_ENV: test

checkout:
  post:
    - '[[ ! -e "$(git rev-parse --git-dir)/shallow" ]] || git fetch --unshallow'

dependencies:
  # Git requires user.email and user.name configured before git commit can be run.
  pre:
    - git config --global user.email "herokuadmin@salsita.com"
    - git config --global user.name "CircleCI @ Salsita"
  override:
    - npm prune
    - npm install
    - bower prune
    - bower install
  cache_directories:
    - node_modules
    - bower_components
    - build

test:
  override:
    - grunt test
    - grunt build
  post:
    - sed -i -e 's/\/node_modules//' .gitignore
    - sed -i -e 's/\/bower_components//' .gitignore
    - sed -i -e 's/\/bin//' .gitignore
    - sed -i -e 's/\/build//' .gitignore
    - git add -A
    - git commit -m "build"

deployment:
  try:
    branch: /try.*/
    commands:
      - git push -f git@heroku.com:mobius-try.git ${CIRCLE_BRANCH}:master
  dev-v2:
    branch: develop-v2
    commands:
      - git push -f git@heroku.com:mobius-mobius-v2-dev.git ${CIRCLE_BRANCH}:master
  qa:
    branch: /release\/.+/
    commands:
      - git push -f git@heroku.com:mobius-qa.git ${CIRCLE_BRANCH}:master
  client:
    branch: client
    commands:
      - git push -f git@heroku.com:mobius-client.git ${CIRCLE_BRANCH}:master
  prod:
    branch: master
    commands:
      - git push -f git@heroku.com:mobius-prod.git ${CIRCLE_BRANCH}:master
  v2:
    branch: v2
    commands:
      - git push -f git@heroku.com:mobius-v2.git ${CIRCLE_BRANCH}:master
