'use strict';

describe('socialLinks directive', function() {

  // TODO: create the test
  /*
  var TEMPLATE = '<social-links/>';
  var TEMPLATE_URL = 'directives/socialLinks/socialLinks.html';

  var TEST_SETTINGS = [
    {'network': 'pinterest', 'link': 'https://pinterest.com'}
  ];

  beforeEach(function() {
    module('mobiusApp.directives.socialLinks', function($provide){
      $provide.value('Settings', {
        UI: {
          socialLinks: TEST_SETTINGS
        }
      });

    });
  });*/

});
