'use strict';
/*
describe('stickable', function() {
  var _$rootScope, _scope, _$compile, _elem;

  var TEMPLATE = '<div stickable="class"/>';

  beforeEach(function() {
    module('mobiusApp.directives.stickable');
  });

  beforeEach(inject(function($compile, $rootScope) {
    _$compile = $compile;
    _$rootScope = $rootScope;

    // Initialization
    _elem = _$compile(TEMPLATE)(_$rootScope);

    _$rootScope.$digest();
    _scope = _elem.scope();
  }));

  describe('when directive is initialized', function() {
    it('should have scope defined', function(){
      expect(_scope).to.be.an('object');
    });
  });
});
*/